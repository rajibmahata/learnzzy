# Production override: `docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d --build`
# Differences from local dev:
# - mongo/redis/MCPs NOT bound to host ports (internal only; no 27018/6379/3001-3003/6333 exposure)
# - MCPs stay private behind the learnzzy network; only Nginx (port 80/443) is public
# - container log rotation (10 MiB x 3 files per service)
# - production env expectations documented below (compose cannot enforce secrets)
#
# Required on the VPS before first deploy (never commit values):
#   ADMIN_EMAIL, ADMIN_PASSWORD_HASH, ADMIN_AUTH_SECRET,
#   TUTOR_MCP_API_KEY, OER_MCP_API_KEY, NCERT_MCP_API_KEY (if enabling MCPs),
#   AI_API_KEY (optional), SENTRY_DSN (optional),
#   NEXT_PUBLIC_APP_URL=https://<production-domain>
services:
  web:
    ports:
      - "127.0.0.1:3000:3000"
    environment:
      NODE_ENV: production
      APP_ENV: production
      # TLS is mandatory in production (see deploy/nginx.prod.conf), so
      # session cookies stay Secure. Never set this to "false" in prod.
      COOKIE_SECURE: "true"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 60s

  tutor-mcp:
    ports: []
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  oer-mcp:
    ports: []
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  ncert-mcp:
    ports: []
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  qdrant:
    ports: []
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  mongo:
    ports: []
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"

  redis:
    ports: []
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
